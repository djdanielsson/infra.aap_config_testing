---
hub_namespaces:
  - name: aap_config_e2e
    company: E2E
    description: Isolated hub namespace for collection E2E
    state: "{{ e2e_state | default('present') }}"
  - name: aap_config_e2e_alt
    company: E2E Alt
    description: Secondary hub namespace for option coverage
    state: "{{ e2e_state | default('present') }}"
